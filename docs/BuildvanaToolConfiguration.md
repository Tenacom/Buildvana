# The Buildvana configuration file

TODO: document `buildvana.json` / `buildvana.jsonc` — discovery, home-directory marking, validation, and every setting.

Until then, two references cover the file: the JSON schema, [`schemas/buildvana.schema.json`](../schemas/buildvana.schema.json), documents every setting with its description and built-in default value; and this repository's own [`buildvana.jsonc`](../buildvana.jsonc) doubles as a worked example, with every setting either set or commented out with the reason it is not needed.
